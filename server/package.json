{
  "name": "@heyhomie/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HeyHomie orders backend — Fastify + Postgres. Authoritative source of truth for orders; implements the OrderGateway HTTP contract.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@heyhomie/api": "*",
    "fastify": "^4.28.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.16.0",
    "typescript": "~5.6.3"
  }
}
