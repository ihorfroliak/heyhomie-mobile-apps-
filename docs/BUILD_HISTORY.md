# Build History

Compact ledger of every build: what shipped + real defects found/fixed. Newest
first. This is the canonical history; [INDEX.md](INDEX.md) links here instead of
inlining it.

| Commit | Build | Shipped | Defects found+fixed (by execution) |
|---|---|---|---|
| `5f27736` | 26 — NotificationPort (invite/reset delivery seam) | `NotificationPort` — the ONE delivery abstraction for capability tokens: `sendInvitation`/`sendPasswordReset` + `nullNotificationPort` (tests) + `consoleNotificationPort` (dev, structured + TOKEN-FREE + masked recipient). Injected via `AuthDeps.notifications` into `buildApp` → `registerAuthRoutes`; `index.ts` wires the console port. The route delivers **best-effort + isolated** (a send failure never fails the auth op nor changes the enumeration-safe response; token-free failure log; no auto-retry). **`makeAuthService` is unchanged — it never learns how email is delivered** (it mints tokens; the route hands them to the port). Contract/OrderGateway/orderContract untouched. | — (feature; gate proves the console port never logs the token; e2e proves delivery fired with the same minted token + failure isolation; pg proves delivery over real pg) |
| `26826bf` | 25 — account disable / enable / delete | Owner account lifecycle in the single `makeAuthService` (no new framework): `User.disabledAt` (migration v8), `AuthService.listMembers/disableUser/enableUser/deleteUser` + `AuthRepo` (`setUserDisabled`/`deleteUserById`/`listUsersByTenant`/`countOwners`/`revokeInvitationsByInviter`). **Disabled** → login/refresh/password-reset all rejected (generic 401 / null; sessions revoked immediately). **Deleted** → sessions + pending invitations revoked, row removed (auth_sessions/password_resets cascade), email freed, tenant intact. Invariants: owner-only; not self; not the last owner; cross-tenant forbidden (shared `ownerTarget` resolver — missing/cross-tenant/self denied identically). Endpoints `GET /auth/users`, `POST /auth/users/:id/{disable,enable}`, `DELETE /auth/users/:id`. `authClient` + admin Members screen. | Accepted tradeoff (not a defect): an already-issued access token stays valid until it expires (≤15 min) after disable — refresh is blocked, so access ends within one access-TTL. Documented. |
| `8928dfe` | 24 — auth operations & account lifecycle | Extended the single `makeAuthService` (no new framework): **invitation mgmt** (`GET /auth/invitations` owner/admin; `POST /auth/invitations/:id/revoke` owner; cannot-revoke-accepted; no token hashes); **password reset** (`/auth/password-reset/{request,confirm}` — opaque sha256 one-time token, enumeration-safe identical response, reset revokes ALL sessions → fresh login); **session mgmt** (`GET /auth/sessions`, `DELETE /auth/sessions/:id` — own-only, no refresh tokens; +`lastUsedAt`/`deviceLabel` metadata). Migration v7 (session cols + `password_resets`). `authClient` + admin Invitations screen + client Password-reset screen. OrderGateway untouched. | Session-revoke would nuke the whole family: the reuse-detector couldn't tell a *rotated* token from a *deliberately-revoked* one → added `revokedReason` (`rotated`\|`revoked`); now revoking one device leaves the rest (gate + pg proven) |
| `8bda9be` | 23 — member invites & per-user accounts | One tenant → many users. `Role` extended `owner`/`admin`/`worker` (kept `member` for back-compat); `register` now mints `owner` (was `admin`). New `Invitation` (sha256 one-time token, expiring, single-use, revocable, tenant-bound) + migration v6 `invitations`; `AuthService.invite`(owner-only)/`accept`(joins the inviter's tenant + logs in)/`revokeInvite`; `pgAuthRepo` + `authClient` (`invite`/`acceptInvite`) extended. Endpoints `POST /auth/invite` (authenticated) + `/auth/accept-invite` (public); `isPublic` narrowed to an explicit pre-auth set so invite is gated. Mobile: admin Invite-member + worker Accept-invite screens. Reuses HMAC/refresh/middleware — no duplicate auth. OrderGateway untouched. | — (feature; gate 32 invite assertions incl. reuse/revoke/expiry/owner-only; e2e + pg over real HTTP/pg) |
| `2546230` | 22 — worker backend integration | Worker app moved off mock `Mission` data onto the real backend, **no contract change**: `lib/store` (kv + expo-secure-store), `_layout` auth bootstrap + login gate, `login` screen, wired logout; jobs list (`(tabs)/missions`) + a new `job/[id]` detail consume `orderGateway` (Order model — `subscribe`/`ordersSnapshot`/`getOrder`/`completeOrder`), env-selected Local/HTTP like client/admin. Reuses `authClient` (no new auth), never shows price/payout (worker privacy). Demo dashboard tabs (index/earnings/schedule + `mission/[id]`) left as-is. e2e extended with a worker-device flow (login→assigned job→complete→paid→logout→reject). | `\bbookingStore\b` in a worker comment tripped the anti-store guard → reworded |
| `8152720` | 21 — mobile production readiness | Auth UX: client login+register screens, admin login, wired logout (profile), and a **route gate** in `_layout` (unauthenticated → `/login`, mirrors the consent gate). **Encrypted token storage**: swapped interim AsyncStorage → **expo-secure-store** (Keychain/Keystore) behind the same `SecureStore` interface — no UI change. Role/tenant stay server-side (apps carry only an opaque token); admin/worker reuse the same auth, no client role handling; worker left offline (not backend-wired). OrderGateway untouched. | Security review: tokens at rest were unencrypted (AsyncStorage) → expo-secure-store; unauthenticated users could reach protected tabs → `_layout` gate. e2e: `type SecureStore` name-clashed the `expo-secure-store` module import → aliased `ExpoSecureStore` |
| `ff444c4` | 20 — end-to-end integration (Local→HTTP) | Apps can now run against the real backend: `orderGateway` selects Local vs `httpOrderGateway` by `EXPO_PUBLIC_ORDERS_API_URL`; new `authClient` turns the async token store into the sync `getToken` the port needs + owns login/register/refresh/logout/bootstrap + `authFetch` (transparent refresh-on-401); app `_layout` startup bootstrap + interim `secureStore`. **No OrderGateway change** — all wiring at the transport/composition layer. New `test:e2e` proves the full journey (register→SSE→refresh→logout→reject) against a real Fastify server. | e2e: two access tokens minted in the same second are byte-identical (HMAC over second-granularity `iat`) → assert the single-use REFRESH token rotated, not the access string; block-comment closed early on a literal `*/` in a path |
| `c0fb69c` | 19 — CI & production hardening | CI now gates the FULL pipeline: split into a fast `checks` job (gate + `typecheck:server` + `test:live`) and a `postgres` service job (`test:pg` + `test:ops`) on a real pg 16; both use locked `npm ci` (was a fragile per-workspace install with no root install). New `verify:full` script = one-command local pipeline. Fresh-clone bootstrap validated (install→check→live). | 3 pre-existing `server/` typecheck errors (route-generic `mutate` handler, `routes.ts`) — fixed (typed as `FastifyRequest<{Params}>`, body cast) so `typecheck:server` can gate; `test:ops` exited 0 even on a rolling-deploy consistency violation → now asserts zero-loss/zero-dup/persist/shared-truth and exits 1 |
| `678321e` | 18 — production auth foundation | Real credential auth issuer: `POST /auth/{register,login,refresh,logout}` (scrypt password + existing HMAC access token + opaque **refresh token** stored sha256-hashed, single-use rotation w/ reuse-detection → revoke family). New pure `makeAuthService` (injected `AuthRepo`+`AuthCrypto`, mirrors `orderService`) + `memoryAuthRepo`; server `authCrypto` (node:crypto scrypt/HMAC) + `pgAuthRepo`; migration v5 (`users`+`auth_sessions`, email/refresh-hash UNIQUE). Replaces dev-only `/dev/token` as the issuer. **OrderGateway contract unchanged**; access-token format unchanged; `buildApp` auth arg optional (back-compat). | login timing side-channel — unknown-email skipped scrypt (enumeration oracle) → constant-work decoy verify (found in review, gate-locked); live 44/44 (real scrypt+HMAC+rotation+reuse+logout), pg 38/38 (hash persisted, unique email, rotation revocation durable across a fresh connection) |
| `6ba3432` | 17 — idempotent create | content-hash `Idempotency-Key` (port auto-sends) + server TTL dedup by (tenant,key) → no duplicate booking on retry/double-tap; no contract change | — (feature; live 28/28) |
| `a777d78` | 16 — validate external review | Reproduced 10 review findings first (`test:repro`), fixed the real ones | C1 SSE disconnect-mid-send leak (cleanup before await); C2 SHUTDOWN_DRAIN_MS→0 (strict parse, fail-fast); C3 double-SIGTERM→exit1 (guard); C4 clock-rollback 429 (clamp); C5 idle-evict full-burst (effective idle window); C7 O(n) sweep/req (throttled). C6/C8/C9 verified by-design |
| `45428db` | deps hygiene | removed unused `@heyhomie/domain` from server; declared `@heyhomie/analytics` client dep (was hoisting-only) | — |
| `d30556b` | 15 — independent audit | fresh-eyes review | metrics label cardinality DoS (unmatched routes); active_requests negative drift (stamp+guard); rate-limiter drained-bucket leak (idle-only evict) |
| `2c8359d` | 14 — ops readiness | k8s graceful shutdown (readiness-flip→drain→bounded close + SSE teardown), rolling-deploy/soak/backup-restore harness | shutdown dropped in-flight OR hung on SSE → readiness-flip pattern; `npx tsx` PID-1 → SIGTERM killed drain (→ `node --import tsx`) |
| `821c9d2` | 13 — load/perf | load harness, EXPLAIN, failure injection | SSE crash under load (`reply.hijack` + guarded writes + headersSent guard) |
| `1cc0ccb` | 12 — hardening H1–H5 | trustProxy, tuned pg pool (statement_timeout), non-root container, versioned migrations, `npm ci` (image 599→366MB) | migration CREATE-TABLE race (moved inside advisory lock) |
| `480cf45` | 11b — docker verified | compose up + build PASS on real pg | compose `AUTH_DEV_MODE=1` vs prod image → crash-loop (→0); healthcheck `localhost`→IPv6 false-unhealthy (→127.0.0.1) |
| `e45cf44` | 11 — pg proof | real Postgres 16/16→25/25 (CAS, DB CHECK, 100-parallel, tenant SQL, migration idempotency) | — |
| `8e0686d` | 10 — live validation | real Fastify on socket, 23/23 | workspace pkgs missing `"type":"module"` (barrel import broke); transport-4xx→500 (`toCanonical`); SSE-safe shutdown |
| `bf933ed` | 09 — verification | stress/reconnect-storm/churn; removed dead exports | fakeBackend dangling service subscription |
| `562b68f` | 06 — observability | Prometheus `/metrics` (zero-dep), correlation ids, structured logs, telemetry | — |
| `66c1616` | 06 — security | token exp+skew, input validation, per-IP rate limit, canonical 401, redaction, bodyLimit | health-probe auth-skip |
| `0af95f9` | 06 — data integrity | optimistic version CAS + idempotent retry + terminal invariants + DB CHECK, 100-parallel/property tests | `dr-${Date.now()}` id collision (→ `uid()`) |
| `d6c6ee1` | 06 — errors | canonical `AppError` hierarchy, no raw leak | — |
| `bb3009f` | 06 — gateway reliability | timeouts, bounded retry+backoff+jitter+budget, dedupe, self-healing SSE | — |
| `9ca3f9a` | 06 — infra | fail-fast config, health probes, graceful shutdown, Docker+compose | — |
| — | 03A–05 (pre-tag) | OrderGateway inversion (frozen contract, store hidden, anti-dep guard); Http adapter + Fastify/pg server; auth + tenant isolation (orthogonal) | `dr-${Date.now()}` collision |
| — | 01 | store persistence seam (`KeyValueStore`) | — |
| `6d5b26f`… | base | 3-app scaffold, domain, mock api, UI kit, GDPR | — |

Pattern across all builds: **execution finds bugs static review misses** — every "verified" build surfaced ≥1 real defect only reproducible by running (signals, concurrency, docker, live HTTP). Full per-build detail in [INDEX.md](INDEX.md) build-log and the git commit bodies.
